{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && pnpm --filter @clickrypt/crypto build && pnpm --filter @clickrypt/web build",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/api/v1/:path*",
      "destination": "${API_URL}/api/v1/:path*"
    }
  ]
}
