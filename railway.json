{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm --filter @clickrypt/crypto build && pnpm --filter @clickrypt/api build && pnpm --filter @clickrypt/api prisma:deploy",
    "watchPatterns": ["apps/api/**", "packages/crypto/**", "pnpm-lock.yaml"]
  },
  "deploy": {
    "startCommand": "pnpm --filter @clickrypt/api start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
