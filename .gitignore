# Dependencies
node_modules/
.pnpm-store/

# Local env files (keep templates)
.env
.env.local
.env.*.local
**/.env
!.env.example

# Build outputs
dist/
.next/
.next-dev/
*.tsbuildinfo
packages/*/dist/
apps/*/dist/

# Logs
*.log
dev.log
test-auth.log
logs/

# OS / Editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Backups / temp
backups/
tmp/
*.tmp

# Allow the API env file to be edited (do not commit secrets)
!apps/api/.env

# Allow the API env file to be edited (do not commit secrets)
!apps/api/.env

# Allow the API env file to be edited (do not commit secrets)
!apps/api/.env

# Allow the API env file to be edited (do not commit secrets)
!apps/api/.env

# Allow the API env file to be edited (do not commit secrets)
!apps/api/.env
